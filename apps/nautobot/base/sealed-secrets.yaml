# Placeholder for sealed secrets
# This file will be generated using the sealed-secrets skill
# Required secrets:
# - postgres-password: PostgreSQL database password
# - redis-password: Redis authentication password
# - secret-key: Django SECRET_KEY (64+ characters)
# - superuser-password: Initial admin user password
#
# To generate:
# 1. Run: sealed-secrets skill workflow
# 2. Secret name: nautobot-secrets
# 3. Namespace: nautobot
# 4. Keys: postgres-password, redis-password, secret-key, superuser-password
---
apiVersion: v1
kind: Secret
metadata:
  name: nautobot-secrets
  namespace: nautobot
type: Opaque
stringData:
  # TEMPORARY - Replace with SealedSecret before committing to Git
  postgres-password: "CHANGEME-generateWithSealedSecrets"
  redis-password: "CHANGEME-generateWithSealedSecrets"
  secret-key: "CHANGEME-generateWithSealedSecrets-minimum-64-characters-required"
  superuser-password: "CHANGEME-generateWithSealedSecrets"
