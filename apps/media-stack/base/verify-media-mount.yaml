apiVersion: batch/v1
kind: Job
metadata:
  name: verify-media-mount
  namespace: media-stack
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  ttlSecondsAfterFinished: 500
  template:
    spec:
      containers:
      - name: test
        image: alpine:latest
        command: ["/bin/sh", "-c", "echo 'GitOps Verification $(date)' >> /mnt/media/gitops_verify.log && echo 'Sleeping for 500s...' && sleep 500"]
        volumeMounts:
        - mountPath: "/mnt/media"
          name: media
      volumes:
      - name: media
        persistentVolumeClaim:
          claimName: media-library-pvc
      restartPolicy: Never
